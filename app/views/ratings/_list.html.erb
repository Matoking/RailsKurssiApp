<table class="table table-hover">
    <tbody>
    <% entries.each do |entry| %>
        <tr>
            <td><%= entry %></td>
            <td><%= round(entry.average_rating) %></td>
        </tr>
    <% end %>
    </tbody>
</table>
